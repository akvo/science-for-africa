"use strict";

/**
 * Strapi v5 Institution Controller
 */

const { createCoreController } = require("@strapi/strapi").factories;

module.exports = createCoreController("api::institution.institution");
